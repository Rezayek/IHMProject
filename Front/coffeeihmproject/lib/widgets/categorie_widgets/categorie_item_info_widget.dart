import 'package:flutter/material.dart';

class CategorieItemInfoWidget extends StatelessWidget {
  const CategorieItemInfoWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}